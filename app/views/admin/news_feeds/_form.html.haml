= form_for [:admin, feed] do|f|
  = render 'shared/error_messages', :target => feed
  %p
    = f.label :url
    = f.text_field :url
  %p
    = f.label :name
    = f.text_field :name
  %p
    = f.submit feed.new_record? ? "Create" : "Update"

